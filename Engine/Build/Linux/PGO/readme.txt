This directory is used for Profile-Guided Optimization files (results from the instrumented runs) for the default UE4 binaries and programs.

When building a code project, the same directory in the project hierarchy (<ProjectName>/Build/Linux/PGO) will be used instead.

